/**
 * Environment configuration for production
 *
 * En producción, cambiar apiUrl a la URL de tu servidor deployed.
 * Ejemplo: 'https://api.tutienda.com'
 */
export const environment = {
  production: true,
  apiUrl: 'https://api.tutienda.com', // Cambiar cuando tengas el servidor en producción
};
